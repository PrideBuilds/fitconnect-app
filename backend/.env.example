# FitConnect Backend Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real credentials to git

# ============================================
# Database Configuration
# ============================================
DB_NAME=fitconnect
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# ============================================
# Django Settings
# ============================================
# CRITICAL: Generate a new secret key for production
# Use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-dev-key-change-in-production

# Set to False in production!
DEBUG=True

# Comma-separated list of allowed hosts
# Development: localhost,127.0.0.1
# Production: yourdomain.com,www.yourdomain.com,api.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# Google Maps API
# ============================================
# Get your API key from: https://console.cloud.google.com/
# Enable: Places API, Geocoding API, Maps JavaScript API
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# ============================================
# Email Configuration
# ============================================
# For development: Use console backend to print emails to terminal
# For production: Use SMTP backend with real email service

# Console backend (development - prints to terminal)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# SMTP backend (production - sends real emails)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@gmail.com
# EMAIL_HOST_PASSWORD=your_app_password_here
# DEFAULT_FROM_EMAIL=FitConnect <your_email@gmail.com>

# Or use SendGrid (production alternative)
# EMAIL_BACKEND=sendgrid_backend.SendgridBackend
# SENDGRID_API_KEY=your_sendgrid_api_key
# DEFAULT_FROM_EMAIL=FitConnect <noreply@yourdomain.com>

# ============================================
# Site Configuration
# ============================================
# Frontend URL (used in emails and redirects)
# Development: http://localhost:5173 (Vite default)
# Production: https://yourdomain.com
SITE_URL=http://localhost:5173
SITE_NAME=FitConnect

# ============================================
# Stripe Payment Processing
# ============================================
# Get your API keys from: https://dashboard.stripe.com/apikeys
# Development: Use test keys (starts with sk_test_ and pk_test_)
# Production: Use live keys (starts with sk_live_ and pk_live_)

STRIPE_SECRET_KEY=sk_test_your_test_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_test_publishable_key_here

# Webhook Secret
# Development: Get from `stripe listen --forward-to http://localhost:8000/api/v1/payments/webhook/ --print-secret`
# Production: Get from Stripe Dashboard → Developers → Webhooks → Add endpoint
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ============================================
# Stream Chat (Real-time Messaging)
# ============================================
# Get your API keys from: https://getstream.io/dashboard/
# Sign up for free tier: https://getstream.io/chat/trial/
STREAM_API_KEY=your_stream_api_key
STREAM_API_SECRET=your_stream_api_secret

# ============================================
# Optional: Cloud Storage (Production)
# ============================================
# For serving user-uploaded files (photos, certifications)
# Development: Use local storage (default)
# Production: Use AWS S3, DigitalOcean Spaces, or similar

# AWS S3 Configuration
# USE_S3=True
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_STORAGE_BUCKET_NAME=fitconnect-media
# AWS_S3_REGION_NAME=us-east-1
# AWS_S3_CUSTOM_DOMAIN=fitconnect-media.s3.amazonaws.com

# DigitalOcean Spaces Configuration (S3-compatible)
# USE_S3=True
# AWS_ACCESS_KEY_ID=your_spaces_access_key
# AWS_SECRET_ACCESS_KEY=your_spaces_secret_key
# AWS_STORAGE_BUCKET_NAME=fitconnect-media
# AWS_S3_ENDPOINT_URL=https://nyc3.digitaloceanspaces.com
# AWS_S3_REGION_NAME=nyc3

# ============================================
# Optional: Monitoring & Logging (Production)
# ============================================
# Sentry for error tracking
# SENTRY_DSN=https://your_sentry_dsn_here

# ============================================
# Optional: Celery (Background Tasks)
# ============================================
# Redis URL for Celery broker (if using background tasks)
# CELERY_BROKER_URL=redis://localhost:6379/0

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in all required values (marked with "your_" placeholders)
# 3. For Stripe webhook secret (development):
#    - Install Stripe CLI: brew install stripe/stripe-cli/stripe
#    - Authenticate: stripe login
#    - Get secret: stripe listen --forward-to http://localhost:8000/api/v1/payments/webhook/ --print-secret
# 4. Never commit .env file to git (it's in .gitignore)
# 5. For production, use secure values and set DEBUG=False

# ============================================
# DOCUMENTATION
# ============================================
# - Stripe Setup: See STRIPE_SETUP.md
# - Webhook Setup: See WEBHOOK_SETUP.md
# - Media Files: See MEDIA_SETUP.md
